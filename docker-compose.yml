version: '3.8'

services:
  odds-app:
    build: .
    container_name: footy_odds_fe
    ports:
      - "8050:8050"  # This ensures Flask is accessible from host
    restart: unless-stopped
    environment:
      - FLASK_ENV=production
    env_file:
      - .env